// import React from 'react'
// import {News} from '../store/slice'

// const TestComponent = () => {

//     const {news, error} = News()
//     console.log(news);
    
//     if (error) {
//         return <div>Error: {error}</div>;
//     }

//     return (
//         <div>
//             {news.length > 0 ? (
//                 <ul>
//                     {news.map((item) => (
//                         <li key={item.id}>{item.title}<br/>{item.content}</li>
//                     ))}
//                 </ul>
//             ) : (
//                 <p>Loading news...</p>
//             )}
//         </div>
//     );
// };


// export default TestComponent
import React from 'react'

const TestComponent = () => {
  return (
    <div>TestComponent</div>
  )
}

export default TestComponent